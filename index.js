// Проблемный JS

// Отлов минимальных ошибок с помощью комментария '@ts-check'
//@ts-check

function addMoney(sum, user) {
    user.money = user.money + summ;
    return user
}

addMoney('1', {name: 'Vlad'});